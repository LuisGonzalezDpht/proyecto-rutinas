// content.md

# 📦 Contenido desde src/assets

Este archivo se carga usando **import dinámico** con el sufijo `?raw`.

## Ventajas de este método:
- ✅ Se incluye en el bundle
- ✅ TypeScript support
- ✅ Mejor performance (no HTTP request)
- ✅ Disponible offline

## Código de ejemplo:

```javascript
// Import dinámico
const loadMarkdown = async () => {
  const module = await import('./assets/content.md?raw')
  return module.default
}
```

> Este método es ideal para contenido estático que no cambia frecuentemente.